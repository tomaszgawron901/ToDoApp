import React, { FC} from 'react';
import { View, Text} from 'react-native';
import styled from 'styled-components/native';

const TodoEditArea = styled.View`
    flex: 1;
    paddingTop: 40px;
`;

const Screen1 = () => {
        return (
            <View>
                <Text> No siema czesc jestem screen1. </Text>
            </View>
        );
};

export default Screen1;